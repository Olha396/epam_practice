/**
 * Function declarations
 */
//keyword 'function'
//identifier 'sum'
// parameters 'a' and 'b' (optional)
// body {}
// return statements (optional, by default return undefined)
// HOISTED

function sum (a, b){
    return a + b;
}
const result = sum(5, 10)
console.log('result is: ', result);

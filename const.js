/**
 * const: Constants are block-scoped such like variables declared using the let keyword.
 * The value of constant cannot be changed through reasignmrnt and it cannot be redeclared.
 */ 

 {
    const a =5;
    console.log(a); 
 }